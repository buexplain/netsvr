syntax = "proto3";

package netsvr.unsubscribe;

option go_package = "internal/protocol;protocol";

//business向worker请求，进行取消订阅操作
message Unsubscribe {
  //目标uniqId
  string uniqId = 1;
  //需要取消订阅的主题
  repeated string topics = 2;
  //取消订阅主题后，需要发给客户的数据，如果没有，可以不传
  bytes data = 3;
}