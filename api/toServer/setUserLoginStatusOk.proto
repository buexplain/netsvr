syntax = "proto3";

package toServerSetUserLoginStatusOk;

option go_package = "./protocol/toServer/setUserLoginStatusOk;setUserLoginStatusOk";

//设置用户登录状态为成功
message SetUserLoginStatusOk {
  uint32 sessionId = 1;
  //存储到网关的用户信息
  bytes userInfo = 2;
  //转发给用户的数据
  bytes data = 3;
}