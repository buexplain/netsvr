syntax = "proto3";

package toServerSubscribe;

option go_package = "./protocol/toServer/reqTopicsSessionId;reqTopicsSessionId";

//获取网关中的某几个主题的session id
message ReqTopicsSessionId {
  repeated string topics = 1;
  //自定义的cmd，网关会原样回传给工作进程
  int32 cmd = 2;
}