syntax = "proto3";

package toServerSubscribe;

import "reCtx/reCtx.proto";

option go_package = "netsvr/internal/protocol/toServer/reqNetSvrStatus;reqNetSvrStatus";

//获取网关的状态
message ReqNetSvrStatus {
  //请求上下文，网关会原样回传给工作进程
  reCtx.ReCtx reCtx = 1;
}