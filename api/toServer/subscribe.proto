syntax = "proto3";

package toServerSubscribe;

option go_package = "netsvr/internal/protocol/toServer/subscribe;subscribe";

//订阅
message Subscribe {
  uint32 sessionId = 1;
  //订阅的主题，可以一次性传递多个
  repeated string topics = 2;
  //转发给用户的数据
  bytes data = 3;
}