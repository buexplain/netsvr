syntax = "proto3";

package toServerPublish;

option go_package = "netsvr/internal/protocol/toServer/publish;publish";

//发布
message Publish {
  //某个主题，网关会根据主题，拿到该主题下的所有session id
  string topic = 1;
  //转发给用户的数据
  bytes data = 2;
}