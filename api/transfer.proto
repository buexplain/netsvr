syntax = "proto3";

package netsvr.transfer;

option go_package = "internal/protocol;protocol";

//worker转发客户发送的信息到business
message Transfer {
  //当前客户的uniqId
  string uniqId = 1;
  //当前客户的session
  string session = 2;
  //客户发送的信息
  bytes data = 3;
}