syntax = "proto3";

package netsvr.singleCast;

option go_package = "internal/protocol;protocol";

//business向worker请求，进行单播
message SingleCast {
  //连接的session id
  uint32 sessionId = 1;
  //需要发给客户的数据
  bytes data = 2;
}