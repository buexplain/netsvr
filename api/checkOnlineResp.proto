syntax = "proto3";

package netsvr.checkOnlineResp;

option go_package = "pkg/protocol;protocol";

//worker响应business，返回网关中已经存在的uniqId
message CheckOnlineResp {
  //worker原样回传给business
  bytes ctxData = 1;
  //已经存在的uniqId
  repeated string uniqIds = 2;
}