syntax = "proto3";

package netsvr.topicListResp;

option go_package = "internal/protocol;protocol";

//worker响应business，返回网关中的主题
message TopicListResp {
  //worker原样回传给business
  bytes ctxData = 1;
  //网关中现存的主题
  repeated string topics = 2;
}