syntax = "proto3";

package netsvr.publish;

option go_package = "internal/protocol;protocol";

//business向worker请求，给某个主题发布信息
message Publish {
  //目标主题
  string topic = 1;
  //需要发给客户的数据
  bytes data = 2;
}