syntax = "proto3";

package netsvr.netSvrStatusReq;

option go_package = "internal/protocol;protocol";

//business向worker请求网关的状态
message NetSvrStatusReq {
  //worker会将该值赋给router.Cmd
  int32  routerCmd = 1;
  //worker会原样回传给business
  bytes ctxData = 2;
}