syntax = "proto3";

package netsvr.infoReq;

option go_package = "pkg/protocol;protocol";

//business向worker请求，获取网关中的info
message InfoReq {
  //worker会将该值赋给router.Cmd
  int32  routerCmd = 1;
  //worker会原样回传给business
  bytes ctxData = 2;
  //目标uniqId
  string uniqId = 3;
}